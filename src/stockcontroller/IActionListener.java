package stockcontroller;

/**
 * This is the interface of the IActionListener.
 */
public interface IActionListener {
  /**
   * Do some actions in our controller.
   *
   * @param description the action in the controller
   */
  void action(String description);
}
